// This deployment is built for one person, so baking their own Team ID
// and league ID in as defaults is the right call — it's the same number
// all season, no reason to make them retype it every visit. Both can
// still be overridden per-browser (My Team) or are just reference points
// (My Leagues) without adding UI complexity for a single-user tool.
export const DEFAULT_TEAM_ID = "2107193";
export const DEFAULT_LEAGUE_ID = 369689;
